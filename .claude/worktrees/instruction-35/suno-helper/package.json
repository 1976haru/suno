{
  "name": "suno-helper",
  "version": "1.0.0",
  "private": true,
  "description": "Local, human-supervised semi-automated field filler for suno.com — fills text fields only, never clicks Create/Generate.",
  "type": "module",
  "scripts": {
    "helper": "node src/helper.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/helper.mjs && node --check src/parseSongs.mjs && node --check src/fillField.mjs && node --check src/sessionQueue.mjs"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
