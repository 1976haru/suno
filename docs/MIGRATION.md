# v2 → v3 마이그레이션 노트

이 문서는 Suno Weaver Studio v2에서 v3로 넘어오면서 무엇이 바뀌었는지, 그리고 사전 검증 리포트(작업 지시문 0장)에 있던 결함들이 실제로 어떻게 처리됐는지 정리한 것입니다.

## 한눈에 보는 변화

- **UI**가 평면 나열식 단일 화면에서 **4단계 위저드**(채널 → 컨셉 → 생성 → 결과)로 바뀌었습니다. 사이드바에서 채널과 저장된 팩을 언제든 오갈 수 있습니다.
- **곡 수**가 10~20곡 고정에서 **1~30곡 자유 선택**으로 바뀌었습니다.
- 30곡을 생성해도 **가사와 제목이 반복되지 않습니다.** 이전 버전은 곡마다 5개 변수만 바뀌는 고정 템플릿이라 사실상 같은 가사였습니다.
- **머니코드**가 5종에서 8종으로 늘었고, 커스텀 코드 진행을 직접 입력할 수 있습니다.
- 생성 결과를 **앱 안에 저장하고 나중에 다시 불러올 수 있습니다** (IndexedDB, 자동 임시저장 포함).
- **AI 평가 에이전트**가 추가되어, Claude/ChatGPT API 연결 시 각 곡을 채점하고 재생성을 제안합니다.
- **설정 화면**에서 AI 제공자, 모델, API 키(서버/로컬 중 선택), 배치 크기를 관리합니다.
- 로컬 개발(`npm run dev`)만으로도 OpenAI/Claude 모드를 테스트할 수 있습니다.
- `vitest` 기반 자동화 테스트와 스트레스 테스트가 추가됐습니다.

## 사전 검증 리포트 항목별 해결 여부

### 🔴 치명적 (Blocker)

| # | 문제 | 상태 | 처리 내용 |
|---|---|---|---|
| B1 | 모든 곡의 가사가 사실상 동일 | ✅ 해결 | `src/core/lyricEngine.ts` 신설. 시드 기반 조합형 생성기로 오프닝/상황/후렴/브릿지/verse2/클로징을 언어별 12~16개 풀에서 뽑아 조합. 30곡 기준 곡 간 가사 라인 자카드 유사도 0.4 미만을 `tests/lyricEngine.test.ts`에서 검증. |
| B2 | 제목 20개 고정 배열, 중복 필연 | ✅ 해결 | `createTitleGenerator()`가 [시간대] + [사물] + [정서어] 조합(수백 개 조합) + 중복 검사로 교체. |
| B3 | 곡 수 10~20 하드 클램프 | ✅ 해결 | `clampSongCount()`를 1~30으로 변경, 슬라이더 + 숫자 입력 + 빠른 선택 칩 추가. |
| B4 | 30곡 요청 시 LLM 응답이 거의 확실히 잘림 | ✅ 해결 | `providers/index.ts`가 6곡(설정 가능) 단위로 배치 분할, 이전 배치의 제목/후렴을 다음 배치에 "재사용 금지" 목록으로 전달. `api/generate.js`의 `safeParseBlueprint()`가 잘린 JSON을 복구 시도 후 실패 시 한국어 에러. |

### 🟠 중대 (요구사항 직결)

| # | 문제 | 상태 | 처리 내용 |
|---|---|---|---|
| M1 | 머니코드 custom 모드에 입력 필드 없음 | ✅ 해결 | `GenerationOptions.customMoneyChord` 필드 추가, custom 선택 시 입력창 노출, `buildStylePrompt()`/LLM 프롬프트에 실제로 주입. |
| M2 | 브라우저에서 API 키 입력 불가 | ✅ 해결 | 설정 모달에 서버 환경변수 모드(기본)와 로컬 브라우저 저장(BYOK) 모드 추가. BYOK 키는 IndexedDB에 저장되고 `X-User-Api-Key` 헤더로 전달. **README의 보안 주의사항 참고.** |
| M3 | LLM 기반 평가/에이전트 없음 | ✅ 해결 | `src/agents/evaluator.ts` 신설. 곡별 6항목 채점(0-100) + 팩 전체 다양성/일관성/구성 순서 평가, 전부 한국어 출력. `local` 제공자에서는 비활성화 안내. |
| M4 | 결과를 앱 안에 저장/재로드 불가 | ✅ 해결 | `src/core/library.ts`(IndexedDB) — 저장, 불러오기, 이름변경, 삭제, 전체 백업 내보내기/불러오기, 자동 임시저장. |
| M5 | 660줄 단일 화면, 흐름 불명확 | ✅ 해결 | 4단계 위저드 + 사이드바로 재구성. `App.tsx`는 239줄로 축소, 나머지는 `src/components/`, `src/hooks/`로 분리. |

### 🟡 개선 필요 (버그·품질)

| # | 문제 | 상태 | 처리 내용 |
|---|---|---|---|
| Q1 | `playlistShort` 선택 시 무조건 8점 감점되는 구조적 오탐 | ✅ 해결 | `buildDurationControl()`의 `playlistShort` 문구에 `no long instrumental break` 추가. |
| Q2 | `'the weekend'` 오타, `저작권`/`원곡` 등 일반 단어 단독 매칭으로 인한 오탐 | ✅ 해결 | `'the weeknd'`로 수정. 한국어 저작권 패턴을 "위험 문맥 구문"으로 좁힘. 부가로, 실제 라이브 테스트에서 발견한 두 가지 오탐도 함께 수정: (1) `famousArtistNames`의 3글자 `'ado'`가 `"shadow"` 같은 일반 단어의 부분 문자열로 매칭되던 문제 → 라틴 문자 이름은 단어 경계(`\b`) 매칭으로 전환. (2) 스코어러가 자기 자신이 생성한 안전 지침 문구(`"avoid ... soundalike vocals"`)를 위반으로 오인하던 문제 → 위험 스캔 전에 `avoid` 절을 제거. |
| Q3 | `scoreSongs()` 중복 호출 | ✅ 해결 | `localGenerator.ts`에서 호출 제거, `providers/index.ts`에서 1회만 호출. |
| Q4 | `lyricImageBank`, `safeLyricRules` 데드코드 | ✅ 해결 | `lyricImageBank`는 영어 제목 생성기의 사물 단어 풀로 사용. `safeLyricRules`는 LLM 시스템 프롬프트에 주입. |
| Q5 | `suno/` 하위 구버전 소스 중복 | ✅ 해결 | 전체 삭제 (루트가 최신/정본이었음을 diff로 확인 후 진행). |
| Q6 | CORS `Allow-Origin` 없음, 레이트리밋/크기제한/타임아웃 없음 | ✅ 해결 | `api/generate.js`에 `Access-Control-Allow-Origin`, 1MB 요청 크기 제한, 30초 `AbortController` 타임아웃, 분당 10회 IP 기반 레이트리밋 추가. |
| Q7 | `package.json` 전체 `"latest"` | ✅ 해결 | 설치된 실제 버전으로 고정. |

### 실사용 중 추가로 발견/수정한 항목 (스펙에는 없었지만 실제 테스트로 드러남)

- **429 재시도 없음**: `openai.ts`/`anthropic.ts`가 레이트리밋 시 즉시 실패했음 → `src/providers/proxyFetch.ts`에 지수 백오프 재시도(최대 3회) 추가.
- **에러 메시지에 원본 JSON이 그대로 노출**: 클라이언트가 서버 에러 응답의 raw text를 그대로 에러 메시지에 붙였음 → 깨끗한 한국어 메시지만 추출하도록 통일.
- **배치 중 실패 시 이전 배치 결과 유실**: 3번째 배치가 실패하면 1·2번째 배치에서 만든 곡까지 전부 사라졌음 → 진행률 콜백에 부분 결과를 유지하고 결과 화면에서 확인 가능하도록 수정 (완전한 "3번째 배치만 재시도" 버튼은 후속 과제로 남음, 아래 참고).

## 알려진 한계 / 후속 과제

- **S13 배치 부분 실패 시 "3번째 배치만 재생성" 버튼**은 구현하지 않았습니다. 현재는 실패한 배치까지의 결과가 화면에 남지만, 다시 생성하면 처음부터 다시 만들어집니다.
- **S8 (IndexedDB 100팩 벌크 저장) 스트레스 테스트**는 자동화하지 않았습니다. Node.js에는 IndexedDB 구현이 없고, 이 하나의 시나리오만을 위해 `fake-indexeddb` 같은 의존성을 추가하는 것은 "의존성 최소화" 원칙과 맞지 않는다고 판단했습니다. 단일 팩의 저장/불러오기는 Playwright로 실제 브라우저에서 확인했습니다.
- **Step2Concept의 머니코드 카드**는 명시된 UX(카드 그리드 + 설명 + 미리보기)를 구현했지만, 100% 아이콘/시각 디자인까지는 다듬지 않았습니다.
